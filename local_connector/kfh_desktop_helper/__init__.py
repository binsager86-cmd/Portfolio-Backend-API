"""Desktop helper: runs the KFH browser-automation connector on the account
owner's own machine so a real KFH login stays on their own network, never a
shared server IP. See app.py for the security boundary and pairing.py for
the one-time pairing nonce.
"""
